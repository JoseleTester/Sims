@model Sim

    <div class="jumbotron">
        <h1 class="display-3">@Model.Name @Model.LastName</h1>
        <p class="lead">Sim of @Model.Gender gender</p>
        <hr class="my-4">
        <p>Currently owns @Model.Money simoleons</p>
        <p class="lead">
            <a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a>
        </p>
    </div>